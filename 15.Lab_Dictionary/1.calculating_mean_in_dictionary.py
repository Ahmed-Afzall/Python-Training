# Defining the dictionary
dict = {"A": 6, "B": 9, "C": 5, "D": 7, "E": 4}

# Getting all the values from the dictionary
values = dict.values()

# Calculating the mean (sum of values divided by number of values)
mean = sum(values) / len(values)

# Printing the result
print("Mean:", mean)