# Defining a list
list = [1, 2, 3, 4, 5]

# Initializing a variable to store the sum (currently 0)
total = 0

# Looping through each item in the list through for loop
for item in list:
    total += item  # Adding each item to total

# Printing the result
print("Sum of the list:", total)