# Defining a list
list = ['red', 'green', 'white', 'black']

# Looping through the list in reverse using a range
for i in range(len(list)-1, -1, -1):
    print(f"Element: {list[i]}, Original Index: {i}") #printing the reversed elements with their original index
